---
summary: "CLI reference for `opencraft clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `opencraft clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `opencraft clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `opencraft clawbot qr` (same behavior as [`opencraft qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `opencraft clawbot qr` -> `opencraft qr`

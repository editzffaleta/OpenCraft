apiVersion: v1
kind: ConfigMap
metadata:
  name: opencraft-config
  labels:
    app: opencraft
data:
  opencraft.json: |
    {
      "gateway": {
        "mode": "local",
        "bind": "loopback",
        "port": 18789,
        "auth": {
          "mode": "token"
        },
        "controlUi": {
          "enabled": true
        }
      },
      "agents": {
        "defaults": {
          "workspace": "~/.opencraft/workspace"
        },
        "list": [
          {
            "id": "default",
            "name": "OpenCraft Assistant",
            "workspace": "~/.opencraft/workspace"
          }
        ]
      },
      "cron": { "enabled": false }
    }
  AGENTS.md: |
    # OpenCraft Assistant

    You are a helpful AI assistant running in Kubernetes.

apiVersion: v1
kind: Service
metadata:
  name: opencraft
  labels:
    app: opencraft
spec:
  type: ClusterIP
  selector:
    app: opencraft
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP

services:
  - type: web
    name: opencraft
    runtime: docker
    plan: starter
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: "8080"
      - key: SETUP_PASSWORD
        sync: false
      - key: OPENCRAFT_STATE_DIR
        value: /data/.opencraft
      - key: OPENCRAFT_WORKSPACE_DIR
        value: /data/workspace
      - key: OPENCRAFT_GATEWAY_TOKEN
        generateValue: true
    disk:
      name: opencraft-data
      mountPath: /data
      sizeGB: 1
